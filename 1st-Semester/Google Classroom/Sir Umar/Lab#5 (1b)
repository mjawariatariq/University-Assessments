Write Python code for a loop that runs infinitely. It only exits when the user presses ‘q’ key.

Ans:

while True:

    user_input = input("Press 'q' to exit: ")

    if user_input.lower() == 'q':

        break